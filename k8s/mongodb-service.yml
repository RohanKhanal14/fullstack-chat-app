# Regular Service for application access
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: chat-app
  labels:
    app: mongodb
    component: service
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
      name: mongodb
  type: ClusterIP